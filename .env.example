# Create a local .env file from this template and use a Web application OAuth client ID
# from Google Cloud Console. Add your local and production origins to that OAuth client.
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
